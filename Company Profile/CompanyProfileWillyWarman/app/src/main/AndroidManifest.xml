<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.companyprofile.willywarman">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.CompanyProfileWillyWarman">

        <activity android:name=".layout.DetailNewsActivity"/>
        <activity android:name=".layout.NewsActivity" />
        <activity android:name=".layout.ComingSoonActivity" />
        <activity android:name=".layout.ContactActivity" />
        <activity android:name=".layout.DashboardActivity" />

        <activity android:name=".layout.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>