<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sekud.id">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".base.Application"
        android:allowBackup="true"
        android:icon="@mipmap/logo_1"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_1"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sekud_id">
        <activity android:name=".feature.DetailItemActivity"></activity>
        <activity android:name=".feature.CreateItemActivity" />
        <activity
            android:name=".feature.MainActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".feature.SplashActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>